{
  "name": "@perimeterchurch/style-tokens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsup && cp -r src/*.css dist/ && cp -r src/themes dist/",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "tailwindcss": "^4",
    "tsup": "^8",
    "typescript": "^5.9.3"
  }
}
